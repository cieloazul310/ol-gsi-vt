import MdxMapLayout from '../../layout/mdxMapLayout';
import gsiOptVtPaleLayer from '../../map/layers/gsiOptVtPale';

export const meta = {
  title: '最適化ベクトルタイル(淡色)',
  layer: gsiOptVtPaleLayer,
  layerId: 'opt-vt-pale',
};

最適化ベクトルタイルを使った淡色レイヤの生成方法です。

`@cieloazul310/ol-gsi-vt` モジュールから `gsiOptVtPaleLayer` 関数をインポートし OpenLayers レイヤーとして定義します。淡色プリセットのスタイルが適用されたレイヤが生成されます。

## Example code

```ts
import { gsiOptVtPaleLayer } from '@cieloazul310/ol-gsi-vt';

const layer = gsiOptVtPaleLayer();

export default layer;
```

export default ({ children }) => <MdxMapLayout meta={meta}>{children}</MdxMapLayout>;
